import React from "react"

import { Footer } from "./Footer"

export default {
  title: "COMPONENTS/C-5 Footer",
  component: Footer,
  argTypes: {},
}

const Template = args => <Footer {...args} />

export const Default = Template.bind({})
Default.args = {}
